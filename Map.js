// Map Data Structure

const data = new Map([
    ["firstName", "anurag"],
    ["lastName", "saini"]
]);

console.log(data.size);
console.log(data.has("firstName"));
console.log(data.get("firstName"));